apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: HelloWorld
spec:
  replicas: 1 
  template:
    metadata:
      labels:
        app:  HelloWorld
    spec:
      containers:
      - name:  HelloWorld-pod
        image: <image>
        ports: 
        - containerPort: 3000
