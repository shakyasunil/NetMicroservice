name: hello-deploy
on:
  workflow_dispatch:
  # push:
  #   branches:
  #   - dev

env:
  SERVICE: HelloWorld
  DOTNET_VERSION: '6.0.401'
  IMAGE_NAME: helloworld
jobs:
  Build_zip:
    runs-on: ubuntu-latest
    # if: ${{ github.event_name == 'pull_request' }}
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    - uses: ./.github/workflows/build
      with:
        service: ${{ env.SERVICE }}
        dotnet_version: ${{ env.DOTNET_VERSION }}
    - name: save artifact
      uses: actions/upload-artifact@v3
      with:
        name: ${{ env.SERVICE }}
        path: ${{ env.SERVICE }}.zip
  BuildContainers:
      runs-on: ubuntu-latest
      needs: Build_zip
      # if: ${{ github.event_name == 'pull_request' }}
      steps:
        - uses: actions/checkout@v3
        - name: Download zip from
          uses: actions/download-artifact@v3
          with:
            name: ${{ env.SERVICE }} 
        - shell: pwsh
          run: | 
            Expand-Archive ./${{ env.SERVICE }}.zip  -DestinationPath ./
        - shell: shell
          run: | 
             ls -al
            docker build -t ${{ env.IMAGE_NAME }}:latest .

      